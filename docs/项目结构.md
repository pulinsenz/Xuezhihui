```
multi-agent-rag-system/
├── docker-compose.yml          # 整体编排：java、python-agent、redis、milvus
├── .gitignore
├── README.md                   # 项目介绍、架构图、启动步骤、接口文档
├── java-backend/               # SpringBoot 业务服务
│   ├── Dockerfile
│   ├── pom.xml
│   └── src/main/java/com/agent/rag
│       ├── RAGApplication.java                 # 启动类
│       ├── config/
│       │   ├── FeignConfig.java                # Feign超时配置（调用Python‑Agent）
│       │   ├── RedisConfig.java
│       │   ├── SseConfig.java                  # SSE流式转发配置
│       │   └── WebConfig.java
│       ├── client/
│       │   └── PythonAgentClient.java          # Feign接口，调用Python Agent服务
│       ├── controller/
│       │   ├── AuthController.java            # 登录鉴权
│       │   ├── KnowledgeController.java        # 知识库管理：上传文档、删除知识库
│       │   ├── ChatController.java             # 对话入口，SSE流式接口
│       │   └── TaskController.java             # 长耗时任务查询接口
│       ├── service/
│       │   ├── impl/
│       │   ├── AuthService.java
│       │   ├── KnowledgeService.java
│       │   ├── ChatService.java                # 组装请求，调用AgentClient
│       │   └── TaskService.java                # 长任务管理，读写Redis任务状态
│       ├── mapper/                             # Mybatis‑Plus，操作MySQL
│       ├── entity/                             # 用户、知识库、对话记录、任务实体
│       ├── dto/
│       │   ├── req/                            # 请求DTO
│       │   └── resp/                           # 返回DTO
│       ├── exception/                          # 全局异常、降级处理
│       └── util/
│           └── SseEmitterUtil.java             # SSE工具
│   └── src/main/resources
│       ├── application.yml
│       └── db/migration/                       # flyway数据库初始化脚本
│
└── python-agent/               # FastAPI + LangGraph Agent服务
    ├── Dockerfile
    ├── requirements.txt
    ├── main.py                 # FastAPI入口，对外提供接口
    ├── .env
    ├── api/
    │   ├── chat_api.py         # 对话接口：普通post、SSE流式接口
    │   └── knowledge_api.py    # 文档向量化入库接口（被Java调用）
    ├── agent/
    │   ├── graph.py            # LangGraph状态图构建，组装5大Agent节点
    │   ├── state.py            # Agent State状态定义
    │   ├── nodes/              # 各个Agent节点实现
    │   │   ├── router_agent.py         # 路由Agent
    │   │   ├── retrieve_agent.py      # 检索Agent（BM25+向量+重排）
    │   │   ├── tool_agent.py          # 工具Agent，回调Java拿业务数据
    │   │   ├── reflect_agent.py       # 反思校验Agent，幻觉校验、判断是否二次检索
    │   │   └── answer_agent.py        # 回答组装Agent
    │   └── tools/              # Agent工具集合
    ├── rag/
    │   ├── chunker.py          # 文档分块（语义分块、重叠窗口）
    │   ├── retriever.py        # 混合检索实现 BM25 + Milvus向量检索
    │   └── reranker.py         # 交叉编码器重排
    ├── llm/
    │   └── llm_client.py       # 大模型统一封装（Ollama / DeepSeek）
    ├── storage/
    │   ├── redis_store.py      # Redis读写：会话记忆、agent状态
    │   └── milvus_store.py     # Milvus向量库封装
    └── utils/
        ├── logger_util.py      # Agent链路埋点日志
        └── exceptions.py
```

## 关键边界说明（写文档 & 面试要讲）

### Java‑backend 职责

1. 用户登录、鉴权、权限校验；
2. MySQL：用户、知识库元数据、对话记录、异步任务状态；
3. 接收前端上传文件，做基础解析，调用 python‑agent 接口做向量化入库；
4. SSE 接收前端，调用 Python SSE 接口，把 token 流式透传给前端；
5. Feign 调用 Agent 普通接口；长耗时任务写入 Redis 消息队列；
6. **Python Agent 需要业务数据，请求 Java 的 controller 接口，带上鉴权信息**。

### python‑agent 职责

1. 只处理 AI 相关逻辑，**不访问 MySQL**；
2. LangGraph 状态图，5 个 Agent 节点流转，支持循环重试；
3. RAG 全套：文档分块、混合检索、重排；
4. 读写 Milvus 向量库；读写 Redis 存放会话、Agent 状态；
5. 提供接口给 Java 调用；必要时回调 Java 接口获取业务数据。

## 核心接口清单（便于写接口文档）

### Java 对外（前端调用）

- `POST /api/auth/login` 登录
- `POST /api/knowledge/upload` 上传知识库文档
- `GET /api/chat/stream?sessionId=xxx` SSE 流式对话
- `POST /api/task/submit` 提交长耗时 Agent 任务
- `GET /api/task/{taskId}` 查询任务结果

### Python Agent 接口（Java 调用）

- `POST /api/agent/chat` 普通对话
- `GET /api/agent/stream?session_id=xxx` SSE 流式 Agent
- `POST /api/knowledge/vectorize` 文档向量化入库

### Python 回调 Java（获取业务数据）

> 示例：`GET /api/business/data/query`，带上 token 鉴权，Java 校验权限后返回业务数据。

## requirements.txt (python-agent 示例)

```
fastapi
uvicorn
langgraph
langchain
langchain‑ollama
langchain‑community
pymilvus
redis
rank_bm25
sentence‑transformers
pydantic-settings
python‑dotenv
```

## pom.xml 关键依赖（java‑backend）

```
<!-- SpringBoot Web、Redis、Mybatis‑plus -->
<!-- OpenFeign 调用python agent -->
<dependency>
    <groupId>org.springframework.cloud.openfeign</groupId>
    <artifactId>spring‑cloud‑starter‑openfeign</artifactId>
</dependency>
<!-- SSE -->
<!-- flyway 数据库版本管理 -->
```