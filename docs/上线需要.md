已经具备（不用再做）
编排：docker-compose.yml 编排 8 个服务，docker-compose.prod.yml 生产覆盖：对外只暴露 80，中间件/后端端口全关，配了日志轮转 + 自动重启
一键部署：deploy.sh 会自动校验 .env 必填密钥（含 TURNSTILE_SECRET_KEY），健康收敛后才完成
安全基线：Python Agent 生产强制真 DeepSeek（没配 key 直接拒绝启动，llm*client.py:25）；JWT_SECRET 无默认值；所有镜像非 root 运行；knife4j 生产已关
登录防爆破：Java 登录已实现 Redis 计数限流 + 账号失败锁定 + IP 限流（LoginAttemptService + Lua 原子计数，默认 5 次/30 分钟锁 15 分钟，IP 20 次/分），AuthControllerTest 覆盖
注册防刷：Cloudflare Turnstile 人机验证（TurnstileService 服务端 siteverify 校验，生产配置 TURNSTILE_SECRET_KEY 后强制，缺失 token 拒绝；前端 AuthForm 渲染 widget 并随注册提交 token）。上传类型白名单：文档限 txt/md/markdown/csv/json/html/xml/pdf/docx，封面/头像限 jpg/jpeg/png/gif/webp/bmp（排除 svg 防 XSS）
默认凭据已清理：admin 初始密码已改为强随机 BCrypt（init.sql 与 backup SQL 同步）；MYSQL_ROOT_PASSWORD / MinIO 凭据已在 .env 生成强随机，minio 与 milvus 已通过 docker-compose.yml 透传同源凭据
同源部署：前端 axios 走相对 /api，Nginx 反代同源，CORS 基本不触发；SSE 的 nginx 缓冲问题已处理
密钥隔离：.env、application-local.yml 都被 gitignore，COS 密钥没提交
上线前必须做（阻塞项）
域名 + HTTPS：国内服务器先域名备案；docker-compose.prod.yml:50-57 的 443 + 证书挂载是占位注释，需在 nginx.conf 加 listen 443 ssl 的 server 块并让 80 跳转。不备案只能裸 IP 走 HTTP。
填生产密钥：cp .env.example .env 填 DEEPSEEK_API_KEY / AGENT_TOKEN / JWT_SECRET / TURNSTILE_SECRET_KEY / VITE_TURNSTILE_SITE_KEY（强随机或 Cloudflare 站点申请），改掉默认 MYSQL_ROOT_PASSWORD=111111 和 MinIO minioadmin（MinIO 改了要按 .env.example 提示同步给 milvus 加环境变量，否则 Milvus 读不了对象存储）。TURNSTILE_SECRET_KEY 是注册人机验证，不配则 deploy.sh 拒绝启动。
生产数据初始化（关键决策）：刚导入的 init.sql 现在带的是开发库快照——5 个用户（含 123123、repro*_/probe\__ 测试账号）、28 个测试会话、108 条消息，且 knowledge_doc.fileUrl 全是 Windows 本地路径 D:\...，换机器全部失效。上线前要么为生产单独准备一份「干净版 init.sql」（只建表 + admin），要么上线后清空业务表。admin 默认密码已替换为强随机 BCrypt（与 backup SQL 同步），上线时用部署前配置的密码登录后建议再改。
服务器规格 + 安全组：Milvus standalone + MinIO + etcd + MySQL + Redis + 3 应用，建议 4C8G 起步（embedding 模型 ~130MB、Milvus 吃内存）；安全组只放行 80/443，其余全关。
CORS 占位域名：application-prod.yml:5 的 your-frontend-domain.com 改成真实域名（同源其实不触发，改了更稳）。
建议做（上线后 / 打磨）
数据备份：目前零备份。加 cron 定时 mysqldump + 备份 file-data、milvus-data、minio-data 卷；文档存储目前只在本地卷，可考虑像封面一样迁 COS 对象存储。
监控告警：至少一个 Uptime Kuma 盯 /healthz 和各容器健康态；Docker 日志已有轮转。
镜像拉取：国内服务器大概率要配 docker-compose.pull-mirror.yml 或 registry mirrors。
CI/CD：目前 deploy.sh 手动跑，可加 GitHub Actions 自动构建推送。
上线 smoke test：/healthz → 登录 admin → 建知识库 → 传文档 → 问答 → 封面。
其中第 3 项（生产数据）直接关系到我们刚做的事，需要你拍板。需要的话我可以直接帮你：生成一份干净版 init.sql、补全 nginx HTTPS 配置，或写一个 MySQL/文件备份脚本。要哪个？
