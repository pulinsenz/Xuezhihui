# 面试准备文档（不入库）
mianshi/

# IDE 配置（不入库）
.idea/
*.iml

# 环境变量（可能含密钥，不入库）
# 注意：.env.example 是模板，必须入库；只忽略真实 .env（含本地变体）
.env
.env.local

# 本地文件存储（开发环境上传的文件）
data/

# 数据库备份/导出（可能含用户密码哈希与隐私，严禁提交；本地副本放 data/backup/）
backup/
memory/
